// Draggable
const draggableSnapshot = {
  isDragging: true,
  draggingOver: 'column-1' // droppableId
};

// Droppable
const droppableSnapshot = {
  isDraggingOver: true,
  draggingOverWith: 'task-1' // draggableId
};
